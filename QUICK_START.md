# ⚡ Быстрый старт после оптимизации

## 🚀 Что было сделано

✅ Оптимизирован **PassportAuthController** (убран self-request, добавлены cookie, транзакции)  
✅ Улучшены **Request классы** (строгая валидация, русские сообщения)  
✅ Оптимизированы **Resources** (условная загрузка, computed поля)  
✅ Созданы **миграции** с индексами и правильной структурой БД  
✅ Создан **Enum для ролей** пользователей  
✅ Добавлены **Middleware** для проверки ролей и активности  
✅ Расширена **модель User** (методы проверки ролей, scopes)  

---

## 📦 Установка/Обновление

### 1. Миграции
```bash
# Откатить старые миграции users и user_roles
php artisan migrate:rollback --step=2

# Применить новые миграции
php artisan migrate

# Заполнить роли
php artisan db:seed --class=UserRoleSeeder
```

### 2. Очистить кеш
```bash
php artisan config:clear
php artisan cache:clear
composer dump-autoload
```

---

## 🔐 Использование ролей

### В маршрутах:
```php
// Только модераторы и выше
Route::middleware(['auth:api', 'role:moderator'])->group(function () {
    // ...
});

// Только админы
Route::middleware(['auth:api', 'role:admin'])->group(function () {
    // ...
});

// Только разработчики
Route::middleware(['auth:api', 'role:developer'])->group(function () {
    // ...
});
```

### В контроллерах:
```php
if ($request->user()->isAdmin()) {
    // Админ или разработчик
}

if ($request->user()->isModerator()) {
    // Модератор, админ или разработчик
}
```

### С Enum:
```php
use App\Enums\UserRole;

$role = UserRole::ADMIN;
$role->isAdmin(); // true
$role->label(); // "Администратор"
```

---

## 🎯 Основные эндпоинты API

### Публичные:
```
POST /api/v1/auth/register     - Регистрация
POST /api/v1/auth/login        - Вход
POST /api/v1/auth/forgot-password - Восстановление пароля
```

### Защищенные (требуют auth:api):
```
GET  /api/v1/auth/user    - Текущий пользователь
PUT  /api/v1/auth/update  - Обновление профиля
POST /api/v1/auth/logout  - Выход
```

---

## 📊 Роли пользователей

| ID  | Роль        | Описание                     |
|-----|-------------|------------------------------|
| 1   | USER        | Обычный пользователь         |
| 500 | MODERATOR   | Модератор                    |
| 900 | ADMIN       | Администратор                |
| 999 | DEVELOPER   | Разработчик (максимум прав)  |

---

## 🛠️ Полезные команды

```bash
# Создать пользователя через tinker
php artisan tinker
User::create([
    'name' => 'Admin',
    'email' => 'admin@example.com',
    'password' => Hash::make('password'),
    'role_id' => 900
]);

# Получить всех админов
User::admins()->get();

# Получить активных модераторов
User::moderators()->active()->get();
```

---

## 📖 Полная документация

Подробная документация: [OPTIMIZATION_GUIDE.md](OPTIMIZATION_GUIDE.md)

---

## ⚠️ Важно!

1. **Обновите существующие маршруты** с использованием новых middleware
2. **Проверьте права доступа** для критичных операций
3. **Протестируйте авторизацию** перед деплоем
4. **Настройте `.env`** для правильной работы cookie (APP_URL, SESSION_SECURE_COOKIE)

---

## 🐛 Проблемы?

Если что-то не работает:
1. Проверьте логи: `storage/logs/laravel.log`
2. Очистите кеш: `php artisan config:clear && php artisan cache:clear`
3. Проверьте миграции: `php artisan migrate:status`
4. Проверьте роли в БД: `SELECT * FROM user_roles;`

---

Готово! 🎉 Система оптимизирована и готова к работе.

