# 📖 Swagger API Документация - Руководство

## ✅ Swagger успешно настроен!

Swagger документация автоматически генерируется из аннотаций в коде и доступна по адресу:

```
http://your-domain.com/api/documentation
```

Для локальной разработки (OSPanel):
```
http://localhost/api/documentation
```

---

## 🚀 Быстрый старт

### 1. Доступ к документации

Откройте браузер и перейдите по адресу:
```
http://localhost/api/documentation
```

Вы увидите интерактивный интерфейс Swagger UI со всеми доступными эндпоинтами API.

### 2. Тестирование API через Swagger

#### Публичные эндпоинты (без авторизации):

1. **Регистрация пользователя**
   - Откройте `POST /api/v1/auth/register`
   - Нажмите "Try it out"
   - Заполните данные:
     ```json
     {
       "name": "Иван Иванов",
       "email": "ivan@example.com",
       "password": "Password123!",
       "password_confirmation": "Password123!"
     }
     ```
   - Нажмите "Execute"
   - Скопируйте `accessToken` из ответа

2. **Вход в систему**
   - Откройте `POST /api/v1/auth/login`
   - Нажмите "Try it out"
   - Заполните данные:
     ```json
     {
       "email": "ivan@example.com",
       "password": "Password123!"
     }
     ```
   - Нажмите "Execute"
   - Скопируйте `accessToken` из ответа

#### Защищенные эндпоинты (требуют авторизации):

1. **Авторизация в Swagger UI**
   - Нажмите на кнопку "Authorize" 🔓 в правом верхнем углу
   - В поле "Value" введите: `Bearer YOUR_ACCESS_TOKEN`
     (замените YOUR_ACCESS_TOKEN на токен, полученный при логине)
   - Нажмите "Authorize"
   - Нажмите "Close"

2. **Тестирование защищенных эндпоинтов**
   - Теперь можно тестировать эндпоинты с замком 🔒
   - Например: `GET /api/v1/auth/user` - получить данные пользователя
   - `PUT /api/v1/auth/update` - обновить профиль
   - `POST /api/v1/auth/logout` - выход

---

## 📝 Структура документации

### Теги (Tags):

- **Authentication** - Все операции с аутентификацией
  - Регистрация
  - Вход
  - Выход
  - Получение данных пользователя
  - Обновление профиля
  - Восстановление пароля

---

## 🔧 Команды для работы со Swagger

### Регенерация документации

После изменения аннотаций в контроллерах:

```bash
php artisan l5-swagger:generate
```

### Публикация конфигурации (если нужно)

```bash
php artisan vendor:publish --provider "L5Swagger\L5SwaggerServiceProvider"
```

### Очистка кеша

```bash
php artisan config:clear
php artisan cache:clear
```

---

## 📋 Добавление новых эндпоинтов в документацию

### Пример аннотации для нового эндпоинта:

```php
/**
 * @OA\Get(
 *     path="/users",
 *     tags={"Users"},
 *     summary="Получить список пользователей",
 *     description="Возвращает список всех пользователей с пагинацией",
 *     security={{"bearerAuth": {}}},
 *     @OA\Parameter(
 *         name="page",
 *         in="query",
 *         description="Номер страницы",
 *         required=false,
 *         @OA\Schema(type="integer", example=1)
 *     ),
 *     @OA\Parameter(
 *         name="per_page",
 *         in="query",
 *         description="Количество на странице",
 *         required=false,
 *         @OA\Schema(type="integer", example=20)
 *     ),
 *     @OA\Response(
 *         response=200,
 *         description="Список пользователей",
 *         @OA\JsonContent(
 *             @OA\Property(property="data", type="array",
 *                 @OA\Items(ref="#/components/schemas/User")
 *             ),
 *             @OA\Property(property="meta", type="object",
 *                 @OA\Property(property="current_page", type="integer", example=1),
 *                 @OA\Property(property="total", type="integer", example=100)
 *             )
 *         )
 *     ),
 *     @OA\Response(response=401, description="Не авторизован")
 * )
 */
public function index(Request $request): JsonResponse
{
    // ...
}
```

### Пример создания новой схемы:

```php
/**
 * @OA\Schema(
 *     schema="Product",
 *     type="object",
 *     title="Product",
 *     description="Модель товара",
 *     @OA\Property(property="id", type="integer", example=1),
 *     @OA\Property(property="name", type="string", example="Товар 1"),
 *     @OA\Property(property="price", type="number", format="float", example=99.99),
 *     @OA\Property(property="description", type="string", example="Описание товара")
 * )
 */
class ProductSchema {}
```

После добавления новых аннотаций:
```bash
php artisan l5-swagger:generate
```

---

## 🎨 Настройка внешнего вида

### Изменить заголовок:

В `config/l5-swagger.php`:

```php
'api' => [
    'title' => 'Ваше название API',
],
```

### Изменить описание API:

В `app/Http/Controllers/Controller.php`:

```php
/**
 * @OA\Info(
 *     title="Ваш API",
 *     version="1.0.0",
 *     description="Описание вашего API"
 * )
 */
```

---

## 🔐 Типы аутентификации

### Bearer Token (используется сейчас):

```php
/**
 * @OA\SecurityScheme(
 *     type="http",
 *     securityScheme="bearerAuth",
 *     scheme="bearer",
 *     bearerFormat="JWT"
 * )
 */
```

Использование в эндпоинте:
```php
/**
 * @OA\Get(
 *     path="/endpoint",
 *     security={{"bearerAuth": {}}}
 * )
 */
```

---

## 📊 Полезные аннотации

### Параметры запроса:

```php
@OA\Parameter(
    name="id",
    in="path",
    description="ID ресурса",
    required=true,
    @OA\Schema(type="integer")
)
```

### Тело запроса:

```php
@OA\RequestBody(
    required=true,
    @OA\JsonContent(
        required={"field1", "field2"},
        @OA\Property(property="field1", type="string"),
        @OA\Property(property="field2", type="integer")
    )
)
```

### Ответы:

```php
@OA\Response(
    response=200,
    description="Успешный ответ",
    @OA\JsonContent(
        @OA\Property(property="data", type="array", @OA\Items(type="object"))
    )
)
```

### Массивы:

```php
@OA\Property(
    property="items",
    type="array",
    @OA\Items(ref="#/components/schemas/User")
)
```

---

## 🐛 Решение проблем

### Swagger UI не отображается

1. Проверьте, что пакет установлен:
   ```bash
   composer show darkaonline/l5-swagger
   ```

2. Очистите кеш:
   ```bash
   php artisan config:clear
   php artisan cache:clear
   php artisan route:clear
   ```

3. Регенерируйте документацию:
   ```bash
   php artisan l5-swagger:generate
   ```

### Документация не обновляется

```bash
php artisan l5-swagger:generate
php artisan config:clear
```

Обновите страницу в браузере с Ctrl+F5 (жесткое обновление)

### Ошибки в аннотациях

Проверьте синтаксис аннотаций:
- Все фигурные скобки закрыты `{}`
- Все круглые скобки закрыты `()`
- Запятые стоят правильно
- Названия схем совпадают

Запустите:
```bash
php artisan l5-swagger:generate
```

В консоли будут показаны ошибки валидации.

---

## 📂 Структура файлов Swagger

```
app/
├── Http/
│   └── Controllers/
│       ├── Controller.php              # Основная информация API
│       └── Api/
│           └── v1/
│               ├── PassportAuthController.php  # Аннотации эндпоинтов
│               └── Schemas/
│                   └── UserSchema.php          # Схемы моделей

config/
└── l5-swagger.php                      # Конфигурация Swagger

storage/
└── api-docs/
    └── api-docs.json                   # Сгенерированная документация
```

---

## 🎯 Лучшие практики

1. **Всегда добавляйте описания** к параметрам и ответам
2. **Используйте примеры** (`example=`) для лучшего понимания
3. **Группируйте эндпоинты** по тегам (`tags={"название"}`)
4. **Документируйте все статус коды** (200, 401, 422, 500)
5. **Создавайте схемы** для переиспользуемых структур
6. **Регенерируйте документацию** после каждого изменения
7. **Тестируйте API** через Swagger UI перед релизом

---

## 🌐 Доступ к JSON документации

Swagger также генерирует JSON файл с документацией:

```
http://localhost/docs
```

Этот файл можно использовать для:
- Импорта в Postman
- Генерации клиентов API
- Интеграции с другими инструментами

---

## 📚 Дополнительные ресурсы

- [OpenAPI Specification](https://swagger.io/specification/)
- [L5-Swagger GitHub](https://github.com/DarkaOnLine/L5-Swagger)
- [Swagger Editor](https://editor.swagger.io/) - для валидации аннотаций

---

## ✨ Готово!

Swagger полностью настроен и готов к использованию! 

Доступ к документации:
```
http://localhost/api/documentation
```

После каждого изменения запускайте:
```bash
php artisan l5-swagger:generate
```

Удачной разработки! 🚀

